import { defineConfig } from 'vite';

export default defineConfig({
  // Relative assets make the build work on GitHub Pages project sites:
  // https://USERNAME.github.io/REPOSITORY/
  base: './',
});
